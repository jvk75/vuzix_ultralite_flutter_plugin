name: vuzix_ultralite_flutter_plugin_example
description: Demonstrates how to use the vuzix_ultralite_flutter_plugin plugin.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  vuzix_ultralite_flutter_plugin:
    path: ../

  cupertino_icons: ^1.0.2

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
